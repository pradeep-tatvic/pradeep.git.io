import { Navbar, Container, Nav } from "react-bootstrap"

const Footer = () => {
    return (
        <Navbar collapseOnSelect expand="lg" bg="dark" variant="dark">
        <Container className="text-white ml-1 mx-auto">

          Footer is showing
        </Container>
      </Navbar>
    )
}
export default Footer